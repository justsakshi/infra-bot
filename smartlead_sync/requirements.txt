gspread>=6.0.0
google-auth>=2.0.0
httpx>=0.27.0
polars>=0.20.0
python-dotenv>=1.0.0
# Asset-tracker / health-history reads and writes (domain_estate,
# blacklist_monitor, placement_store). Previously absent, which made every
# Mongo path silently no-op inside the container.
pymongo>=4.6.0
